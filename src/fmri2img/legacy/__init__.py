"""
Compatibility namespace for legacy scripts and module surfaces.

The repository's canonical workflows now live under ``fmri2img.workflows`` and
the official decoder stack lives under the shared/private model modules. Older
surfaces remain importable elsewhere in the package for comparison studies and
historical reproduction, but should be treated as legacy unless explicitly
referenced by the current docs.
"""
